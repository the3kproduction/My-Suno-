services:
  - type: web
    name: suno-discord-bot
    env: node
    buildCommand: npm install
    startCommand: node final_render_bot.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production